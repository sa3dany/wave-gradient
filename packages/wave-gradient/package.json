{
  "name": "wave-gradient",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/wave-gradient.js",
  "exports": {
    ".": "./dist/wave-gradient.js"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/sa3dany/wave-gradients.git",
    "directory": "packages/wave-gradient"
  },
  "scripts": {
    "build": "node scripts/esbuild.js",
    "build:shaders": "npm run minify:shaders",
    "dev": "run-p dev:shaders dev:lib",
    "dev:lib": "node scripts/esbuild.js --watch",
    "dev:shaders": "npm run minify:shaders -- --watch",
    "lint": "eslint src",
    "minify:shaders": "node scripts/minify-shaders.js",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "clang-format": "^1.7.0",
    "esbuild": "^0.14.39",
    "eslint-plugin-jsdoc": "^39.2.0"
  }
}
