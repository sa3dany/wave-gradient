{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --no-cache --parallel --continue",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^4.3.0",
    "prettier": "^2.6.1",
    "turbo": "^1.1.10"
  }
}
